namespace Winspot_App.Strings;

public static class SettingsStatusMessages
{
    public const string ReservedWindowsHotkey = "That hotkey is reserved by Windows. Choose a different chord.";

    public const string CaptureValidation = "Capture numeric settings must be positive, within limits, and default duration must not exceed max duration.";
}
